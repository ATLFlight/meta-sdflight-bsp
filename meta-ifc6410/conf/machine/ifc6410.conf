#@TYPE: Machine
#@NAME: ifc6410

#@DESCRIPTION: Machine configuration for ifc6410 systems

# Ship all kernel modules by default
MACHINE_EXTRA_RRECOMMENDS = "kernel-modules"
MACHINE_EXTRA_RDEPENDS += "compat-wireless"

# Uncomment the following line to enable the hard floating point abi. Note that
# this breaks some binary libraries and 3D (neither of which ship with
# meta-yocto). For maximum compatibility, leave this disabled.
DEFAULTTUNE ?= "cortexa8hf-neon"
include conf/machine/include/tune-cortexa8.inc

MACHINE_ARCH = "arm"
HOST_OS = "linux-gnueabihf"
DPKG_ARCH = "armhf"

SERIAL_CONSOLE = "115200 ttyHSL0"
LK_ROOT_DEV = "/dev/sda3"

PREFERRED_PROVIDER_virtual/kernel ?= "linux-qr-ifc6410"
PREFERRED_PROVIDER_virtual/wlan-module ?= "compat-wireless"

QRL_MACHINE_MODULES = "compat-wireless"
#MACHINE_FEATURES = "usbgadget usbhost vfat alsa"

# Change the compiler
GCCVERSION = "4.7%"
